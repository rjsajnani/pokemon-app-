import { StyleSheet } from 'react-native';

export const CardStyle = StyleSheet.create({
  container: {
    height: 120,
    backgroundColor: 'black',
    borderRadius: 10,
    padding: 10,
    marginVertical: 5,
  },
  text: { color: 'white' },
});
